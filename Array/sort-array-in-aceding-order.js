// in following programe we see how we can sort array in acending order

// we have unsorted array
var array = [1, 96, 52, 7, 3, 75, 32];
var res = [];
// console.log(array.sort());
for (let i = 0; i < array.length; i++) {
  console.log(array[i]);

  //   if (array[i] < array[i + 1]) {
  //     // res.push(array[i]);
  //     // i++;
  //   }
}
